__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

# generated output: caches, renders, screenshots. All regenerable, none committed.
out/

# downloaded map tiles (~11MB), rebuilt by `make tiles`
web/tiles/
# folded out of tables by `make data`
web/js/generated.js

# derived is generated from tables, so most of it is ignored.
# port_distances.json is the exception and is committed: rebuilding it needs
# `make tiles` (11MB) and a minute of work, and everything downstream of the
# sea chart needs only that one file.
derived/port_tasks.json
derived/port_tasks.csv
