"""OSRS Sailing port tasks: the data pipeline, the map, and the route search.

`tables/` is the hand-edited truth; this package reads it, checks it against
the wiki, and folds it into what the app and the route search consume.
`paths` names every file involved, anchored to the repo. See README.md.
"""
