"""The ground truth: what the game and the map actually are.

Nothing here is a modelling choice - it is measured, pasted from the wiki, or
read off the map. What any of it *costs* is `../problem/params.py`.

    catalogue.py  what exists: ports, tasks, and the xp table
    distances.py  how far apart the ports are, in game tiles
    survey/       the one-off job that measured those distances
"""
